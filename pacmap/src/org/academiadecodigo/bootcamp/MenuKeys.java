package org.academiadecodigo.bootcamp;

import org.academiadecodigo.simplegraphics.keyboard.KeyboardEvent;
import org.academiadecodigo.simplegraphics.keyboard.KeyboardHandler;

public class MenuKeys implements KeyboardHandler {


    @Override
    public void keyPressed(KeyboardEvent keyboardEvent) {

    }

    @Override
    public void keyReleased(KeyboardEvent keyboardEvent) {

    }

    /*
            KeyboardEvent pressN = new KeyboardEvent();
        pressN.setKeyboardEventType(KeyboardEventType.KEY_PRESSED);
        pressN.setKey(KeyboardEvent.KEY_N);
        kb.addEventListener(pressN);

        KeyboardEvent pressQ = new KeyboardEvent();
        pressQ.setKeyboardEventType(KeyboardEventType.KEY_PRESSED);
        pressQ.setKey(KeyboardEvent.KEY_Q);
        kb.addEventListener(pressQ);
     */
}
